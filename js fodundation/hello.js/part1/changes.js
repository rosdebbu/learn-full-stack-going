let gamename = "call of duty";

gamename = "pubg";

console.log(gamename);

const username = "debjit";

// username = "das"; // This will throw an error because 'username' is a constant and cannot be reassigned.

// username = "das";
// console.log(username);